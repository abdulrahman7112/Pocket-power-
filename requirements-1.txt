# ════════════════════════════════════════
#  Pocket Option Bot — requirements.txt
#  Python 3.10+
# ════════════════════════════════════════

# ── Kivy (واجهة التطبيق) ──
kivy==2.3.0
kivy-deps.sdl2==0.8.0
kivy-deps.glew==0.3.1

# ── Buildozer (بناء APK) ──
buildozer==1.5.0
Cython==0.29.37

# ── طلبات HTTP (Claude AI) ──
requests==2.31.0

# ── حسابات فنية ──
numpy==1.26.4

# ── Android (تُستخدم فقط داخل APK) ──
pyjnius==1.6.1
android

# ── اختياري: Telegram إشعارات ──
# python-telegram-bot==20.7
